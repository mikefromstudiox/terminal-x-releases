/**
 * seed.js — intentionally empty.
 * All default data (categorias_servicio, secuencias_ncf, configuracion)
 * is handled via INSERT OR IGNORE in schema.sql.
 * No demo data is inserted — the FirstTimeSetup wizard handles first-run config.
 */
module.exports = function seed(_db) {}
